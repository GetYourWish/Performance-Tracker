{
  "name": "@performance-tracker/mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Performance Tracker for Android (Expo / React Native). Phase 9 will add the app; today this workspace hosts the dual-runtime drift guard: the SAME golden-fixture contract that runs under Node/Vitest must produce identical results under jest-expo (Hermes).",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "@performance-tracker/core": "1.0.0",
    "react": "19.2.8",
    "react-native": "0.87.1"
  },
  "devDependencies": {
    "expo": "57.0.17",
    "jest": "^29.7.0",
    "jest-expo": "57.0.5",
    "@react-native/assets-registry": "0.87.1"
  }
}
